// Dependencies
import SwiftUI

struct AccountView: View {
    // MARK: - PROPERTIES
    
    // MARK: - BODY
    var body: some View {
        Text("Account view")
    }
}

// MARK: - PREVIEW
struct AccountView_Previews: PreviewProvider {
    static var previews: some View {
        AccountView()
    }
}
